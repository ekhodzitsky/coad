# Project Contracts

These contracts apply COAD to this repository itself.

They are separate from `templates/` because templates are intentionally skipped
by repository-wide validation. Each file here describes a real project module
and points to that module's local `README.md` and `TODO.md` context.
