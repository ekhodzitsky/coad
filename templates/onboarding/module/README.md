# module

Purpose: replace with what this module owns.

Public API:

- Replace with public or agent-relevant surfaces.

Consumers: replace with known callers or workflows.

Invariants: replace with the promises agents must preserve.
