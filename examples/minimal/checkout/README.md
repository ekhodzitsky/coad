# checkout

Owns checkout decision logic for the minimal COAD example.

Agents changing this module should preserve the `CheckoutDecision` surface
unless the task contract explicitly includes consumer migration proof.
