# Example Agent Guide

This example follows COAD.

Minimum onboarding:

1. Paste this guide into `AGENTS.md`.
2. Add one `MODULE_CONTRACT.md`.
3. Ensure the module has `README.md` and `TODO.md`.
4. Run `coad check .`.
