# parallel-work/docs

Purpose: represent a documentation leaf workcell that can be edited by one
write agent while another agent works in `api/`.

Owned files are under `docs/`. API implementation work is delegated to the API
leaf workcell.

Proof:

```bash
coad check examples/parallel-work
```
