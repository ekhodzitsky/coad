# COAD Onboarding Example

This is the smallest complete COAD adoption shape:

- `AGENTS.md` tells agents to run `coad check .`;
- `MODULE_CONTRACT.md` describes one real module;
- `checkout/README.md` gives module context;
- `checkout/TODO.md` gives current module work.

Run from this directory:

```bash
coad check .
```
